# Prism Code

TypeScriptコードをインタラクティブなフローチャートで視覚化するVSCode拡張機能

## ✨ 機能

- 🔍 TypeScript/JavaScriptコードの解析
- 📊 React Flowによるインタラクティブなフローチャート表示
- 🎨 カスタムノード（関数、if文、ループ等）
- 🔄 関数、if文、for文、while文の完全サポート
- 🌐 言語非依存の中間表現（IR）アーキテクチャ

## 使い方

### 1. 依存関係のインストール

```bash
pnpm install
cd webview-ui && pnpm install
```

### 2. ビルド

```bash
# Extension側のビルド
pnpm run compile

# WebView UI側のビルド
pnpm run compile:webview
```

### 3. デバッグ実行

1. VSCodeでこのプロジェクトを開く
2. **F5キー**を押してデバッグを開始
3. 新しいVSCodeウィンドウが開く
4. サイドバーの**Prism Codeアイコン**をクリック
5. TypeScriptファイルを開く（例: `sample/sample.ts`）
6. コマンドパレット（**Cmd+Shift+P**）または右クリックメニューで `Prism Code: コードを可視化` を実行

### 4. フローチャートの確認

- サイドバーにインタラクティブなフローチャートが表示されます
- **ズーム/パン**: マウスホイール、ドラッグで操作
- **ミニマップ**: 全体像を確認
- **ノードの色分け**:
  - 🟢 緑: 開始ノード
  - 🔴 赤: 終了ノード
  - 🔵 青: プロセス（変数、式）
  - 🟣 紫: Return文
  - 🟡 黄: 変数宣言
  - 🩷 ピンク: If文（判定）
  - 🩵 シアン: ループ（For/While）

## 実装状況

### ✅ Phase 1: コア機能（完成）

- ✅ コード解析（TypeScript Parser）
- ✅ AST生成
- ✅ IR（中間表現）への変換
- ✅ React Flowによるビジュアル表示
- ✅ インタラクティブなフローチャート
- ✅ カスタムノード（開始/終了/プロセス/判定/ループ）
- ✅ 基本的な制御構造のサポート（if, for, while）

### 🚧 Phase 2: 高度な機能（今後）

- ⏳ ノードクリックでソースコードへジャンプ
- ⏳ コード編集時の自動更新
- ⏳ より複雑な構文のサポート（クラス、async/await）
- ⏳ レイアウトアルゴリズムの改善

### 🎯 Phase 3: 双方向編集（将来）

- ⏳ フローチャートからコード生成
- ⏳ AI統合（Gemini API）
- ⏳ リアルタイム同期
- ⏳ 他言語対応

## アーキテクチャ

```
┌──────────────┐    ┌─────────────┐    ┌──────────────┐    ┌────────────┐
│ TypeScript   │ -> │ TypeScript  │ -> │      IR      │ -> │   React    │
│ Source Code  │    │   Parser    │    │ Transformer  │    │    Flow    │
└──────────────┘    └─────────────┘    └──────────────┘    └────────────┘
                          ↓                    ↓                    ↓
                        AST               IR (JSON)         Flowchart UI
```

### 疎結合設計

- **IParser**: 言語別パーサーのインターフェース
- **IR**: 言語非依存の中間表現
- **FlowGenerator**: IRをReact Flow形式に変換

## 開発

```bash
# 監視モードでコンパイル（Extension側）
pnpm run watch

# 1回だけコンパイル
pnpm run compile

# WebView UIの開発サーバー起動
cd webview-ui && pnpm run dev
```

## 技術スタック

- **Extension**: TypeScript, ts-morph, VSCode Extension API
- **WebView UI**: React, React Flow, Vite, Dagre（レイアウト）
- **ビルドツール**: TypeScript Compiler, Vite

## ライセンス

MIT
